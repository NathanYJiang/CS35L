# Backend environment variables.
# Copy this file to `.env` and adjust as needed.

# Port the Express server listens on. Defaults to 5001 when unset
# (see server.js).
PORT=5001

# NOTE: Firebase Admin credentials are NOT read from here. Place the service
# account JSON at backend/serviceAccountKey.json (gitignored). See README.
